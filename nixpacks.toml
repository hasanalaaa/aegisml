[phases.build]
cmds = ["pip install -r services/scan-engine/requirements.txt"]

[start]
cmd = "export PATH=/opt/venv/bin:$PATH && cd services/scan-engine && uvicorn main:app --host 0.0.0.0 --port $PORT"
