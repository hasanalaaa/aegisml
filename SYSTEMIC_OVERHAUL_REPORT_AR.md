# تقرير التدقيق الشامل والإصلاح المنهجي — AegisML

> [!WARNING]
> **هذا سجل تاريخي وليس تدقيقًا حاليًا أو موافقةً على النشر.** يصف حالة
> المستودع بتاريخ 2026-07-01، وقد تكون المسارات والنتائج والادعاءات الواردة
> أدناه قديمة. العقد الحالي وحدوده موثّقان في [README.md](README.md)، وخطة
> القبول وسجل التحقق الحالي في
> [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md).

**التاريخ:** 2026-07-01
**النطاق:** الواجهة الخلفية `services/scan-engine` (FastAPI + SQLAlchemy) والواجهة الأمامية `apps/web` (Next.js)
**ملاحظة نطاق:** الطلب أشار إلى مشروع "Forge" بتقنية Prisma وأساس `requireWorkspaceMember` — هذه المكوّنات غير موجودة في هذا المستودع. التدقيق أُجري على الكود الفعلي لمشروع AegisML، ولم يُمسّ نظام التصميم (Obsidian & Gold) إطلاقًا.

---

## أولًا: الثغرات الأمنية الحرجة المكتشفة والمُصلَحة

### 1. استيلاء كامل على أي حساب عبر `/auth/sync` (حرجة — الأخطر في المنصة)
كان المسار `POST /auth/sync` بلا أي مصادقة، وهو يُصدر Access Token و Refresh Token لأي بريد إلكتروني يُرسَل إليه. أي مهاجم كان يستطيع طلب توكن لحساب أي مستخدم (بما فيهم مديرو النظام) بمجرد معرفة بريده.
**الإصلاح:** حماية المسار بسرّ مشترك `AUTH_SYNC_SECRET` يُرسل في ترويسة `X-Auth-Sync-Secret` ويُقارن بـ `hmac.compare_digest` (مقاومة لهجمات التوقيت). عُدّلت `apps/web/auth.ts` لإرسال السر من بيئة الخادم. إن لم يُضبط المتغير يُسجَّل تحذير حرج في السجلات.
**إجراء مطلوب منك:** ضبط `AUTH_SYNC_SECRET` بنفس القيمة في بيئة Railway (الخلفية) و Vercel (الأمامية) فورًا.

### 2. قبول Refresh Token مكان Access Token (حرجة)
`get_current_user` كانت تقبل أي JWT صالح دون التحقق من نوعه، فأصبح توكن التحديث (صلاحيته 30 يومًا) قابلًا للاستخدام كتوكن وصول (المفترض 15 دقيقة)، ما يُبطل فائدة قصر عمر الجلسة.
**الإصلاح:** رفض أي توكن يحمل `type: "refresh"` في مسارات المصادقة، مع التحقق أيضًا من أن الحساب `is_active`.

### 3. مفتاح توقيع JWT احتياطي ثابت ومعروف (حرجة)
عند غياب `SECRET_KEY` كان النظام يستخدم النص الثابت `fallback-secret-for-development-only`، ما يسمح لأي شخص بتزوير توكنات صالحة في أي بيئة نُشرت دون ضبط المتغير.
**الإصلاح:** توليد سرّ عشوائي مؤقت لكل عملية تشغيل (`secrets.token_urlsafe(64)`) مع تسجيل تحذير حرج — التزوير أصبح مستحيلًا حتى لو نُسي المتغير.

### 4. مسار `/auth/refresh` وهمي (خلل وظيفي + أمني)
كان يُعيد النص الحرفي `"new_access_token"` دون أي تحقق.
**الإصلاح:** تنفيذ حقيقي — فك تشفير توكن التحديث، التحقق من نوعه وصلاحيته ووجود المستخدم ونشاطه، ثم إصدار Access Token جديد.

### 5. ثغرة SSRF في تسجيل الـ Webhooks (عالية)
كان بإمكان أي مستخدم تسجيل Webhook يشير إلى عناوين داخلية (`http://169.254.169.254`، `localhost`، الشبكات الخاصة) فيُجبر الخادم على إرسال طلبات إلى بنيته الداخلية.
**الإصلاح:** حارس `_assert_public_url` يحلّ اسم المضيف ويرفض أي عنوان خاص/محلي/محجوز/متعدد البث، مع قصر البروتوكول على http(s) والتحقق من قائمة أحداث صالحة.

### 6. مسارات محمية تنهار أو تتصرف بشكل غير معرَّف بدون مصادقة (متوسطة)
مسارات `developer/webhooks` و `growth/referral` كانت تَستقبل `current_user = None` فتنهار بـ AttributeError (خطأ 500) بدل رفض الطلب.
**الإصلاح:** اعتماد تبعية جديدة `require_user` تُعيد 401 نظيفًا، وتطبيقها على جميع المسارات المتأثرة. أُضيف أيضًا مسار `DELETE /api/v1/developer/webhooks/{id}` مع فرض الملكية (كان الحذف مستحيلًا سابقًا).

### 7. إزالة راوتر Webhooks الوهمي (تقليص سطح الهجوم)
`webhooks.py` كان راوترًا تجريبيًا بلا مصادقة (`/api/v1/webhooks/*`) يُعيد بيانات مزيفة ويُطلق طلبات خادمية نحو httpbin.org. أُزيل تسجيله من التطبيق؛ الإدارة الحقيقية الموثّقة في `/api/v1/developer/webhooks` لم تتأثر (لا استخدام له في الواجهة الأمامية — تم التحقق).

### 8. تسريب تفاصيل الاستثناءات الداخلية
رسالة فشل تحليل الذكاء الاصطناعي كانت تُضمِّن نص الاستثناء الخام (`AI analysis failed: {e}`) في استجابة API. استُبدلت برسالة عامة مع إبقاء التفاصيل في السجلات فقط.

---

## ثانيًا: أخطاء المنطق والأداء المُصلَحة

### 1. استهلاك ذاكرة كارثي في رفع الملفات
`scan_file` كانت تقرأ الملف كاملًا في الذاكرة (`await file.read()`) — حتى 5GB لمشتركي Pro. بضع عمليات رفع متزامنة كانت كفيلة بإسقاط الخادم (OOM).
**الإصلاح:** بثّ الرفع إلى القرص بمقاطع 1MB مع فرض حد الحجم أثناء البث، والتحقق من الـ Magic Bytes من أول 16 بايت، وتنظيف الملف المؤقت في كل مسارات الفشل.

### 2. تجاوز سعة عمود `file_size` (خلل بيانات مؤكد)
العمود كان `Integer` (حده الأقصى ≈ 2.1GB في PostgreSQL) بينما تسمح الخطط برفع 5GB — أي عملية رفع كبيرة كانت ستفشل عند الكتابة في قاعدة البيانات. حُوِّل إلى `BigInteger`. (للجداول القائمة في الإنتاج يلزم `ALTER TABLE scans ALTER COLUMN file_size TYPE BIGINT;`).

### 3. تسميم متبادل لذاكرة التخزين المؤقت للإحصائيات
`/ws/stats` و `/api/v1/stats` كانا يتشاركان مفتاح كاش واحد مع بنيتي JSON مختلفتين، فكل طرف يفسد كاش الآخر ويُعيد للعملاء بيانات بهيكل خاطئ. أصبح للبث الحي مفتاح مستقل `aegisml:livestats` بمدة 15 ثانية.

### 4. حذف الكاش فور كتابته
في نهاية `_process_scan` كان الاستدعاء `invalidate_scan()` يمسح نتيجة الفحص التي كُتبت للكاش قبل سطور قليلة، فيذهب أول طلب `GET /scan/{id}` دائمًا إلى قاعدة البيانات بلا داعٍ. أصبح الإبطال يقتصر على كاش الإحصائيات (الذي يتغير فعلًا بالفحص الجديد).

### 5. تعليق أبدي في بث SSE
`/api/v1/scan/{id}/stream` كان ينتظر رسائل تقدّم إلى الأبد إذا اشترك العميل بعد اكتمال الفحص (لا رسائل ستصل أبدًا)، مع تراكم مشتركين أشباح في `ConnectionManager` (تسريب ذاكرة). الآن: إن كانت النتيجة موجودة تُبثّ رسالة `complete` فورًا وتُغلق القناة؛ وإلا يُفتح بث مع نبضات keepalive كل 25 ثانية وسقف زمني مطلق 30 دقيقة يضمن تحرير الموارد.

### 6. انهيار مسار حالة المهام الخلفية
`/api/v1/scan/{job_id}/status` كان يتحقق بـ `hasattr` فقط بينما القيمة قد تكون `None` (عند غياب Redis) فينهار بخطأ 500. أصبح يُعيد 503 واضحًا.

### 7. تسريب ملفات مؤقتة في فحص الروابط
في `_process_url_scan` كان مسار الملف المؤقت يُسجَّل بعد اكتمال التنزيل، فأي فشل أثناء التنزيل (تجاوز الحجم، انقطاع الشبكة) يترك الملف على القرص للأبد. يُسجَّل المسار الآن قبل بدء البث فيُنظَّف دائمًا.

### 8. فهارس قاعدة بيانات جديدة
أُضيف فهرس مركّب `(is_public, created_at)` لخدمة `/scans/recent`، وفهرس على `risk_level` لخدمة تجميعات `/stats` — أهم استعلامين على أكبر جدول.

---

## ثالثًا: تحسينات الواجهة الأمامية

- **`auth.ts`:** إرسال `X-Auth-Sync-Secret` في مزامنة NextAuth (مكمّل للإصلاح الحرج رقم 1).
- **`lib/byok-crypto.ts`:** كانت `decryptString` تَعِد بإرجاع `""` عند الفشل لكنها ترمي استثناءً فعليًا إذا فشل `subtle.decrypt` (مفتاح مُدوَّر أو نص مُعدَّل) فتكسر صفحة الإعدادات. أصبحت تُعيد `""` في جميع حالات الفشل.

## رابعًا: التحقق

جميع ملفات بايثون المعدّلة (`main.py`, `database.py`, `auth/utils.py`, `auth/router.py`, `routers/developer.py`, `growth/router.py`) اجتازت فحص الترجمة النحوية، وتعديلات TypeScript محصورة وآمنة نوعيًا. لم يُغيَّر أي سلوك واجهة مستخدم أو تصميم.

## خامسًا: توصيات متبقية (لم تُنفَّذ عمدًا لأنها قرارات نشر/بنية)

1. **ضبط `AUTH_SYNC_SECRET` و `SECRET_KEY` في الإنتاج فورًا** — أهم خطوة تشغيلية بعد هذا الإصلاح.
2. `connect_args={"ssl": False}` يعطّل TLS لاتصال PostgreSQL — مقبول داخل شبكة Railway الخاصة فقط؛ فعِّل TLS إن كان الاتصال يعبر شبكة عامة.
3. مفاتيح المنصة في `users.api_key` مخزّنة نصًا صريحًا (بعكس جدول `api_keys` المُجزّأ) — يُنصح بتجزئتها بنفس الأسلوب.
4. تفعيل قيود على `logout` لإبطال Refresh Tokens فعليًا عبر جدول `refresh_tokens` (حاليًا شكلي).
5. اعتماد Alembic لتطبيق `BIGINT` والفهارس الجديدة على قاعدة الإنتاج (إنشاء الجداول التلقائي لا يعدّل الجداول القائمة).
