[build]
builder = "nixpacks"

[deploy]
startCommand = "export PATH=/opt/venv/bin:$PATH && cd services/scan-engine && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
