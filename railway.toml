[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir services/scan-engine"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
